on:
  push:
    branches:
      - main
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@main
      - name: down
        uses: MitchWind/ftp-deploy@master
        with:
          server: ${{ secrets.ftp_host }}
          username: ${{ secrets.ftp_username }}
          password: ${{ secrets.ftp_password }}
          local-dir: Next-Portifolio/
          server-dir: /home/github/Portifolio
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**

      - name: 📂 Sync files
        run: |
            docker compose up -d
